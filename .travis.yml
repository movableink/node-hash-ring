language: node_js

node_js:
  - "10"
  - "12"
  - "14"

# New docker container based test
sudo: false
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8

install:
  - yarn install --frozen-lockfile

script:
  - yarn test
